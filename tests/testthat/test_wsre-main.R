context("wsre method")

# uncomment both if we want to see the trace messages
# cat("\n")
# futile.logger::flog.threshold(futile.logger::TRACE)

# defaults are actually fine for testing? 
wsre_obj <- wsre()